import { tv } from "tailwind-variants";

export const title = tv({
    base: "text-[18px] lg:text-[24px] font-bold text-primaryLC"
})
export const subtitle = tv({
    base: "text-[24px] leading-[1.3em] lg:text-[32px] font-bold"
})
export const text = tv({
    base: "text-[16px] lg:text-[18px] mt-2"
})
